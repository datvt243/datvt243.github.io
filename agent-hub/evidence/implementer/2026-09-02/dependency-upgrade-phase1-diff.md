# 2026-09-02 — dependency-upgrade-phase1 (diff)

- Worker: implementer
- Version: 1.1.0
- Node: `dependency-upgrade-phase1` (`haven/diagrams/dev-loop.prime-mermaid.md`)
- Plan: `evidence/implementer/2026-09-02/dependency-upgrade-phase1-plan.md`

## Diff
| File | Why |
|---|---|
| `package.json` | 11 version-range bumps: `nuxt` `^3.13.0`→`^3.21.11`, `dotenv` `^16.4.5`→`^16.6.1`, `@iconify-json/fe` `^1.2.0`→`^1.2.4`, `@iconify-json/grommet-icons` `^1.2.0`→`^1.2.4`, `@nuxt/eslint` `^1.16.0`→`^1.17.0`, `@vue/runtime-core` `^3.5.8`→`^3.5.42`, `autoprefixer` `^10.4.20`→`^10.5.4`, `postcss` `^8.4.47`→`^8.5.26`, `sass` `^1.79.3`→`^1.99.0`, `sass-embedded` `^1.79.3`→`^1.100.0`, `typescript` `^5.6.2`→`^5.9.3`, `vue-tsc` `^2.1.6`→`^2.2.12`. No other `package.json` field touched. |
| `package-lock.json` | Regenerated by `npm install` (large diff — `nuxt` alone spans 8 minor versions, pulling in new/updated transitive deps: +349/-211/~158 packages on the `nuxt` step, +31/-16/~64 on the second step). `lockfileVersion` unchanged (`3`) — confirmed before/after, this isn't a format churn, it's real dependency-tree movement. |
| `agent-hub/doctrine/domains/PROJECT.md` | New Traps row: npm 10.8.2 (bundled with Node 20) throws an internal arborist error (`Cannot read properties of null (reading 'edgesOut')`) resolving `nuxt@3.21.11`'s peer set — run the install under Node ≥24/npm 11 instead. |
| `agent-hub/haven/diagrams/dev-loop.prime-mermaid.md` | New node row, `IN_PROGRESS` (implementer state; verifier owns the SEALED transition). |

No app code (`.vue`/`.ts` logic) touched — pure dependency-version diff,
matches `SmallestDiff`.

## Command
```
npm install nuxt@3.21.11          # step 1, isolated (highest-risk bump)
npm install postcss@8.5.26 @iconify-json/fe@1.2.4 @iconify-json/grommet-icons@1.2.4 \
  @nuxt/eslint@1.17.0 @vue/runtime-core@3.5.42 autoprefixer@10.5.4 dotenv@16.6.1 \
  sass@1.99.0 sass-embedded@1.100.0 typescript@5.9.3 vue-tsc@2.2.12   # step 2
```
Both run under `nvm use 24` (Node `v24.19.0`, npm `11.17.0`) — the
default shell npm (`10.8.2`) failed step 1 outright (see plan note's
Investigation #4).

```
rm -rf node_modules/.cache .nuxt .output && npm run build
```
Run twice (once after each install step). Output (tail, both runs):
`[nitro] ✔ You can preview this build using node .output/server/index.mjs`
/ `✨ Build complete!`, exit `0` both times (confirmed via `echo $?`
under `set -o pipefail`, not inferred from tail output alone).
`grep -iE "error"` over the full captured log, excluding real asset
filenames (`error-*.mjs`) — no real hits either run.

```
npm run lint
```
(under `nvm use 24`)

Output (tail, after both install steps):
```
✖ 32 problems (0 errors, 32 warnings)
```
Exact match to the recorded baseline (`visit-tracking-client-call` and
every node before it) — 0 new warnings from either bump.

```
npm audit
```
Before (recorded in the sealed `dependency-upgrade-plan` note, 2026-08-30):
`53 vulnerabilities (4 low, 8 moderate, 35 high, 6 critical)`.
After (this pass): `25 vulnerabilities (1 low, 2 moderate, 19 high, 3
critical)`.

```
npm ls unhead @unhead/vue
```
Output:
```
nuxt-app@1.1.0 /Users/_david/Workspace/Project/resume/datvt243.github.io
└─┬ nuxt@3.21.11
  ├─┬ @nuxt/nitro-server@3.21.11
  │ └── @unhead/vue@2.1.17 deduped
  └─┬ @unhead/vue@2.1.17
    └── unhead@2.1.17
```
`npm audit`'s full output no longer contains the string `unhead` anywhere
— the one genuinely runtime-reachable advisory from the plan's
classification is confirmed gone.

## Real UI check via Chrome CDP
`/browser` found no debuggable Chrome running (`curl -s
http://localhost:9888/json/version` returned nothing) — launched a fresh
instance (`open -na "Google Chrome" --args --remote-debugging-port=9888
--user-data-dir="$HOME/.chrome-debug-profile"`), confirmed up via the
same curl check.

Curl smoke-check (all 5 main routes) against a fresh preview server
(`PORT=3992 node .output/server/index.mjs`, backgrounded with `nohup`/
`disown` after an earlier foregrounded attempt got killed by its own
2-minute command timeout): `/` → 200, `/github` → 200, `/contact` → 200,
`/projects` → 200, `/blogs` → 200 (took 14.5s — the external blog API on
Render free tier cold-starting, not a regression; confirmed body has
`<title>Blog</title>` and real content, 247KB).

Wrote a fresh `puppeteer-core` script (`.tmp-cdp-check.mjs`, deleted after
use) that connects via `puppeteer.connect({ browserURL:
'http://localhost:9888' })`, sets a 1400×900 viewport, navigates to `/`
with `waitUntil: 'networkidle2'`, reads the hydrated body text, then does
a REAL click-based SPA navigation (not `page.goto`/`Page.navigate`, which
would bypass client-side routing) to `/github` via a discovered
`a[href="/github"]` link, scrolled into view before clicking. Verbatim
result:
```
ROOT hydrated body text (first 200): "_datvt243\n_resume\n_projects\n_github\n_blogs\n_contact\nVI\n\nXin chào! Tôi là _\n\nVÕ TẤN ĐẠT {\n\n> Frontend Developer\n\nPERSONAL-INFO\nabout-me.md\nskills.ts\nexperiences.pug\neducations.json\nlanguages.json\nSEARC"
URL after click-nav to /github: http://localhost:3992/github
GITHUB page body text (first 200): "_datvt243\n_resume\n_projects\n_github\n_blogs\n_contact\nVI\n\n_daniel\n\ndatvt243\n\nRoses are red, Violets are blue, unexpected '; ' on line 243\n\nHo Chi Minh City, Vietnam\n\n1 người theo dõi\n·\n0 đang theo dõi\nL"
CONSOLE ERRORS: []
```
Real content on both pages (resume hero on `/`, real GitHub profile bio
on `/github`), the click-based SPA nav actually changed the URL and
content (not a full reload), 0 console errors — no hydration mismatch or
runtime break from the 8-minor-version `nuxt` jump.

Cleanup: preview server killed by PID (`kill 60444`, confirmed via
`ps aux | grep index.mjs` → no stray listener). Temp script
(`.tmp-cdp-check.mjs`) deleted. `git status --short` re-checked after
cleanup — only the 4 real changes above remain.

## Acceptance
| Criterion | Evidence |
|---|---|
| All 11 Phase 1 packages bumped to live-verified targets, `puppeteer-core` untouched | `package.json` diff above, matches every target from the plan's Investigation #2 |
| `unhead`/`@unhead/vue` advisory resolved | `npm audit` before/after counts + `npm ls unhead @unhead/vue` above — string `unhead` no longer appears in `npm audit`'s output at all |
| `npm run build` clean, cold cache | Command/Output above, exit 0 both install steps, real `set -o pipefail`-checked exit code |
| `npm run lint` clean, unchanged baseline | Command/Output above, `32 problems (0 errors, 32 warnings)` — exact match |
| Real UI check via CDP | Command/Output above — hydration, real click-nav, 0 console errors |

## Noticed, not done
- Phase 2 (`@nuxt/icon`, `@nuxt/image`, `@nuxt/ui` same-major bumps) and
  Phase 3 (majors: `@nuxtjs/i18n`, `@nuxt/ui`+`tailwindcss`, `pinia`+
  `@pinia/nuxt`, `@nuxt/icon`/`@nuxt/image` v2, `vue-router`,
  `puppeteer-core` research) are explicitly out of scope for this node —
  tracked as separate future issues per the plan doc's suggested execution
  order.
- 25 vulnerabilities remain after this pass — per the plan's own
  classification (and re-confirmed: the remaining ones trace to Nuxt's own
  build/dev tooling — `tar-fs`, `koa`, etc. — never shipped in
  `.output/server`), none of the remaining ones are runtime-reachable in
  production. Not re-audited exhaustively here since the plan doc already
  did that classification; would need re-doing if `npm audit`'s dependency
  paths look different after a future Phase 2/3 bump.
- The npm-10.8.2-arborist-bug workaround (run installs under Node
  ≥24/npm 11) is now a documented trap, but no `engines`/CI enforcement
  was added to force it automatically — out of scope for this node
  (`SmallestDiff`), flagged in case a future session wants to harden it.

## Seal gate
No outward-facing action taken — no `commit`/`push`. Diff + evidence
ready for the verifier pass; deferred to `/ship`/`/todo`'s commit step
after SEAL.
