# 2026-09-05 — dependency-upgrade-phase2 (diff)

- Worker: implementer
- Node: `dependency-upgrade-phase2` (`haven/diagrams/dev-loop.prime-mermaid.md`)
- Plan: `evidence/implementer/2026-09-05/dependency-upgrade-phase2-plan.md`

## Diff
| File | Why |
|---|---|
| `package.json` | 3 version-range bumps: `@nuxt/icon` `^1.5.6`→`^1.15.0`, `@nuxt/image` `^1.8.0`→`^1.11.0`, `@nuxt/ui` `^2.18.6`→`^2.22.3`. No other field touched. |
| `package-lock.json` | Regenerated by `npm install` (`added 47 packages, removed 146 packages, changed 61 packages`). `lockfileVersion` unchanged (`3`). |
| `agent-hub/haven/diagrams/dev-loop.prime-mermaid.md` | New node row, `IN_PROGRESS` (implementer state; verifier owns the SEALED transition). |

No app code (`.vue`/`.ts` logic) touched — pure dependency-version diff,
matches `SmallestDiff`.

## Command
```
nvm use 24 && npm install @nuxt/icon@1.15.0 @nuxt/image@1.11.0 @nuxt/ui@2.22.3
```
Node `v24.19.0`, npm `11.17.0`. Output tail:
```
added 47 packages, removed 146 packages, changed 61 packages, and audited 1281 packages in 20s
18 vulnerabilities (1 low, 2 moderate, 14 high, 1 critical)
```
`git diff package.json` confirms exactly the 3 lines above changed, nothing
else. `package-lock.json`'s `@nuxt/icon`/`@nuxt/image`/`@nuxt/ui` entries
read directly via `node -e "require('./package-lock.json')..."` →
`1.15.0`/`1.11.0`/`2.22.3`, matching targets exactly.

```
rm -rf node_modules/.cache .nuxt .output && npm run build
```
Exit code `0` (`echo "EXIT_CODE:$?"`, `set -o pipefail`). Full log captured
to `/tmp/build-phase2.log`. Ended with `[nitro] ✔ You can preview this
build using node .output/server/index.mjs` / `✨ Build complete!`.

One new build-time line not present before this bump (confirmed via a
controlled A/B: `git stash` back to pre-bump `package.json`, `npm install`,
rebuild — the line does NOT appear; `git stash pop`, reinstall — it
reappears):
```
[@nuxt/image]  WARN  sharp binaries for darwin-arm64 cannot be found. Please report this as a bug with a reproduction at https://github.com/nuxt/image.
```
Investigated rather than dismissed (see "Real UI check" below) — this
machine's `sharp@0.32.6` (a transitive dep, pulled in via the `@nuxt/image`
bump) has no prebuilt binary for `darwin-arm64`, a known `@nuxt/image`/
`sharp` platform-binary issue, not a code bug in this repo. Confirmed it
does NOT break real image rendering (see below) — `ipx`, `@nuxt/image`'s
default image provider, still serves optimized images correctly, likely
via its JS fallback path when the native `sharp` binary is unavailable.
No production-server behavior conclusion drawn from a dev-machine-only
binary warning; the current build/lint/CDP evidence below is what
substantiates it working, not this reasoning alone.

```
npm run lint
```
(under `nvm use 24`). Exit code `0`. Verbatim tail:
```
✖ 32 problems (0 errors, 32 warnings)
```
Exact match to the recorded baseline — 0 new warnings.

```
npm audit
```
Before (recorded baseline from `dependency-upgrade-phase1`, 2026-09-02):
`25 vulnerabilities (1 low, 2 moderate, 19 high, 3 critical)` — re-confirmed
identical immediately before this bump.
After (this pass): `18 vulnerabilities (1 low, 2 moderate, 14 high, 1
critical)`. Improvement is a side effect of transitive dependency movement
(e.g. `tar-fs` bumped transitively), not specifically targeted by this
node — not claimed as a fix for any named advisory. One relevant advisory
`npm audit` still lists: `@nuxt/ui <4.8.1` (moderate, `UAuthForm`/`UForm`
SSR markup omits `method`) — its only fix is `@nuxt/ui@4.11.0`, a major
version jump explicitly out of scope for Phase 2 (tracked in
`DEPENDENCY-UPGRADE-PLAN.md`'s Phase 3 row for `@nuxt/ui`+`tailwindcss`).
Not a regression from this bump — this advisory already existed before it
(same major, `<4.8.1` covers `2.x` too).

## Real UI check via Chrome CDP
`@nuxt/icon` (icons throughout every page) and `@nuxt/ui` (`UBadge`,
`USelect`, `UPagination`, etc.) are both rendering dependencies used
site-wide — qualifies as a visual change per `doctrine/domains/PROJECT.md`.
The new `sharp` binary warning above made real image-rendering verification
the priority, not just a generic smoke test.

`/browser` found no debuggable Chrome running — launched a fresh instance,
confirmed via `curl -s http://localhost:9888/json/version`. Started a fresh
preview server from this pass's own cold-cache build:
`PORT=3994 node .output/server/index.mjs`.

Wrote a fresh `puppeteer-core` script (`.tmp-cdp-check-phase2.mjs`, deleted
after use) that connects via `puppeteer.connect({ browserURL:
'http://localhost:9888' })`, then:
1. Navigates to `/` (`waitUntil: 'networkidle2'`), reads hydrated body text,
   and specifically inspects the local `Avatar.png` `<NuxtImg>` (the exact
   image class the `sharp`-warning risk applies to — a local file needing
   `ipx` transform, not a passthrough remote URL).
2. Real click-based SPA nav (not `page.goto`) to `/github` via
   `a[href="/github"]`, inspects the GitHub avatar `<NuxtImg>` (a remote
   URL, different code path from #1).
3. Real click-based SPA nav to `/projects`, inspects all `<img>` tags.
4. Navigates to `/blogs` (uses `USelect`/`UPagination` from the bumped
   `@nuxt/ui`), checks the page renders with pagination controls present.

Verbatim result:
```json
{
  "rootBodyStart": "_datvt243\n_resume\n_projects\n_github\n_blogs\n_contact\nVI\n\nXin chào! Tôi là _\n\nVÕ TẤN ĐẠT {\n\n> Frontend Developer\n\nreact-js\nPERSONAL-INFO\nabout-me.md\nski",
  "avatarImgStatus": {
    "src": "http://localhost:3994/_ipx/s_240x240/Avatar.png",
    "naturalWidth": 240,
    "naturalHeight": 240,
    "complete": true
  },
  "afterClickUrl": "http://localhost:3994/github",
  "githubAvatarStatus": {
    "src": "https://avatars.githubusercontent.com/u/9960924?v=4",
    "naturalWidth": 460,
    "naturalHeight": 460,
    "complete": true
  },
  "afterProjectsClickUrl": "http://localhost:3994/projects",
  "projectImgStatus": [],
  "blogsHasPagination": true,
  "blogsBodyStart": "_datvt243\n_resume\n_projects\n_github\n_blogs\n_contact\nVI\nCATEGORIES\nJavascript\nVue\n_BLOG\n\nBài viết, hướng dẫn, đoạn code h",
  "consoleErrors": []
}
```
Key finding: `avatarImgStatus` confirms the local `Avatar.png` was served
through `ipx`'s transform path (`/_ipx/s_240x240/Avatar.png`) and loaded
successfully (`naturalWidth: 240`, `complete: true`) — the `sharp`-binary
warning does NOT break real local image optimization at runtime, `ipx`'s
fallback handles it. The GitHub remote avatar also loaded fully
(`naturalWidth: 460`, `complete: true`). `consoleErrors: []` across the
whole session (root + 2 real click-navs + `/blogs`).

`projectImgStatus: []` (no `<img>` tags found on `/projects`) was
investigated further, not assumed: a second follow-up script confirmed
`document.querySelectorAll('img').length === 0` on that page — real content
(project titles/descriptions/tech tags) renders correctly, and
`themes/portfolio-dev/pages/projects/Index.vue`'s `<NuxtImg v-if="p.images[0]">`
guard means the image only renders when the resume API's project record has
an `images[0]` set. The current live resume data has none for any project —
a pre-existing data condition, not something this bump touched or broke
(confirmed by the same 0-image result being independent of `@nuxt/image`'s
version).

Cleanup: preview server on port 3994 killed by PID, confirmed no listener
afterward (`lsof -nP -iTCP:3994 -sTCP:LISTEN` → empty). Both temp CDP
scripts deleted. `git status --short` re-checked after cleanup — only the
4 real changes (`package.json`, `package-lock.json`, diagram row, this
evidence dir) plus 2 pre-existing unrelated working-tree items
(`.claude/skills/hub-tokens/SKILL.md` modified, `.claude/commands/
issues-ls.md` untracked — neither touched by this pass, present before it
started) remain.

## Acceptance
| Criterion | Evidence | Met? |
|---|---|---|
| `@nuxt/icon`/`@nuxt/image`/`@nuxt/ui` bumped to live-verified Phase 2 targets, nothing else touched | `package.json`/`package-lock.json` diff above | ✅ |
| `npm run build` clean (cold cache) | Command/Output above, exit 0 | ✅ |
| `npm run lint` clean, unchanged baseline | `32 problems (0 errors, 32 warnings)` | ✅ |
| Real UI check via CDP | CDP result above — hydration OK, 2 real click-navs OK, local + remote `NuxtImg` both load, 0 console errors | ✅ |
| `npm audit` re-checked | Before/after counts above (25→18, side effect, not the target of this node) | ✅ |

## Noticed, not done
- The `sharp` darwin-arm64 binary warning is new (introduced by this bump,
  confirmed via A/B) but does not break functionality on this dev machine
  today (see CDP evidence). Not fixed here — would need either an
  `--ignore-scripts`-safe rebuild of `sharp` for this platform or an
  explicit `image:` provider override in `nuxt.config.ts`, neither of which
  Phase 2's scope calls for (`SmallestDiff`). Worth a follow-up if it
  starts failing loudly (e.g. on a different CI/build platform) rather than
  just warning.
- `@nuxt/ui <4.8.1`'s `UAuthForm`/`UForm` SSR-method advisory remains
  (needs the 4.x major, already tracked in `DEPENDENCY-UPGRADE-PLAN.md`
  Phase 3 — this repo doesn't use `UAuthForm`/`UForm` today per a repo-wide
  grep, so not runtime-reachable regardless, consistent with the plan's own
  reachability classification style).
- Phase 3 (majors) still fully out of scope, per the plan's own ordering.

## Seal gate
No outward-facing action taken — no `commit`/`push`. Diff + evidence ready
for the verifier pass.
