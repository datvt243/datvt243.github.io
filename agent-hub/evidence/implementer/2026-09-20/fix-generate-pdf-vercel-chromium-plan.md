# 2026-09-20 — fix-generate-pdf-vercel-chromium

**Worker:** implementer
**Version:** 1.0.0 (dev-loop diagram)
**Node:** `fix-generate-pdf-vercel-chromium` (new — no prior node matched, created per `pick_next.md`'s "No diagram matches yet" branch)
**Task:** Fix issue #193 — `/api/generate-pdf` ("Download CV" button, both Hero and About tab) returns 500 in production. Root cause: `server/api/generate-pdf.ts` only knew how to find Chrome via `PUPPETEER_EXECUTABLE_PATH` or an OS-detected desktop path (macOS/Windows/generic Linux) — none of which exist on Vercel's serverless (Amazon Linux) runtime, which has no system Chrome installed anywhere. Discovered during a post-incident production re-check, unrelated to the sanitize-html outage (this file was untouched by any commit today until this fix).

## Hub bytes before: 122475

## Acceptance criteria
1. On Vercel (`process.env.VERCEL` set), Puppeteer launches using a Linux-x64 Chromium binary that actually exists in that runtime.
2. Local dev (and any other non-Vercel host) keeps working exactly as before — `PUPPETEER_EXECUTABLE_PATH` override, then OS-detected fallback path — unchanged behavior, unchanged code path.
3. `npm run build` clean, `npm run lint` clean, baseline unchanged.
4. **Disclosed limitation, not swept under the rug**: `@sparticuz/chromium`'s binary is Linux-x64 only and cannot execute on this macOS dev machine, so the Vercel-specific branch cannot be end-to-end verified locally — only its resolution logic (does the right branch get chosen, does `chromium.executablePath()`/`chromium.args` resolve without throwing at import/build time) can be checked here. Per issue #193's own explicit requirement (itself written to avoid repeating today's earlier sanitize-html incident), the actual Vercel runtime behavior will be verified against a **real Vercel preview deployment** generated by this fix's own PR, before it's allowed to merge into `staging`/`main` — this is an explicit exception to the normal "implementer+verifier seal, then push" order, made necessary by the fact that neither role can access a real serverless Linux environment from this session.

## Files to touch
- `server/api/generate-pdf.ts`
- `package.json`, `package-lock.json` (new dependency: `@sparticuz/chromium`)

No env var needed for the fix itself (Vercel already sets `process.env.VERCEL` automatically; `PUPPETEER_EXECUTABLE_PATH` remains optional/local-host-only after this fix).
