# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log
# ...except agent-hub's committed evidence trail (doctrine/MEMORY.md:
# "evidence/ is committed" — *.log above would otherwise silently drop it)
!agent-hub/evidence/worker-runs.log
!agent-hub/evidence/worker-runs-archive.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

.vscode

# Claude Code: ignore local/personal config, but keep shared custom commands/skills tracked
.claude/*
!.claude/commands/
!.claude/skills/