/**
 * Author: Đạt Võ - https://github.com/datvt243
 * Date: `--/--`
 * Description:
 */

export interface CodeLine {
  html: string
  /** Indent depth in characters (ch), applied as CSS padding so wrapped text stays aligned. */
  indent?: number
}
